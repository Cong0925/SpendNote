# 修改记录 - 2026-07-06

## 评分模块

**提问：** `[09:30]`
1. 用户要求检查并修改评分功能
2. 描述设计：点击"给个好评" → 判断是否评价过 → 未评价则打开评价界面，已评价则进入展示界面

**修复：标签点击无反应问题** `[09:35]`
- 问题：WXML 模板中使用 `selectedTags.includes(item.id)` 判断选中状态，微信小程序模板表达式对数组方法支持不稳定
- 方案：在 JS 中预处理数据，给每个标签添加 `isActive` 属性，WXML 中使用 `item.isActive` 替代
- 改动文件：
  - miniprogram/pages/rating/rating.js - 添加 `initTags()` 和 `updateTagsActiveState()` 方法
  - miniprogram/pages/rating/rating.wxml - 修改标签样式绑定为 `item.isActive`

**修复：标签点击自动添加描述到评论** `[09:40]`
- 需求：选择标签时自动在评论框添加对应描述（换行分隔），取消时移除描述
- 实现：
  - 为每个标签添加 `desc` 字段（更丰富的描述内容）
  - 添加 `addTagDesc()` 方法：添加描述到评论
  - 添加 `removeTagDesc()` 方法：从评论移除描述
  - 点击标签时自动更新评论内容
- 标签描述内容：
  - 界面美观：界面设计精美，配色舒适，看着就很舒服！
  - 操作简单：操作简单易上手，功能一目了然，非常方便！
  - 功能实用：功能实用又齐全，记账需求都能满足！
  - 运行流畅：运行流畅不卡顿，使用体验非常顺畅！
  - 数据安全：数据安全有保障，隐私保护做得很好！
  - 完全免费：完全免费无广告，良心软件必须支持！

**修复：评价展示界面重构** `[09:45]`
- 需求：
  1. 去除顶部感谢评价卡片
  2. 我的评价在列表内置顶显示，带修改/删除按钮
  3. 点击"给个好评"直接进入展示界面
  4. 未评价时在列表顶部显示"添加评价"按钮
  5. 每条评价添加点赞/踩按钮

- 云函数改动 (`cloudfunctions/rating/index.js`)：
  - 新增 `updateRating` - 更新评价
  - 新增 `deleteRating` - 删除评价（验证所有权）
  - 新增 `likeRating` - 点赞（互斥：点赞会取消踩）
  - 新增 `dislikeRating` - 踩（互斥：踩会取消点赞）
  - 修改 `listRatings` - 我的评价置顶，返回点赞/踩状态

- 前端改动 (`miniprogram/pages/rating/rating.js`)：
  - 添加 `isEditing` - 编辑模式状态
  - 添加 `myRating` - 我的评价数据
  - 添加 `ratingTexts` - 评分文字映射（可在 WXML 中访问）
  - 添加 `loadData()` - 统一加载数据
  - 添加 `onAddRating()` - 进入添加评价模式
  - 添加 `onCancelEdit()` - 取消编辑
  - 添加 `onDeleteRating()` - 删除评价（带确认弹窗）
  - 添加 `onLike/onDislike()` - 点赞/踩
  - 修改 `onSubmit()` - 支持新增和修改评价

- 界面改动 (`miniprogram/pages/rating/rating.wxml`)：
  - 页面结构：stats-showcase → filter-section → review-list
  - review-list 内第一个位置：未评价显示"添加评价卡片"，已评价显示"我的评价"
  - 我的评价头部右侧显示"修改/删除"按钮（蓝色/红色标签）
  - 编辑模式显示评分/标签/评论/图片上传区域
  - 每条评价（非自己的）添加👍👎按钮
  - 去除感谢评价卡片
  - 评分统计和筛选栏始终显示

- 样式改动 (`miniprogram/pages/rating/rating.wxss`)：
  - 添加评价卡片样式（蓝色渐变背景，在 review-list 内）
  - 我的评价样式（蓝色边框+顶部渐变，在 review-card 内）
  - 添加 action-btn 修改/删除按钮样式
  - 添加取消按钮样式
  - 添加点赞/踩按钮样式

- 数据库新增字段（自动创建）：
  - `likes` - 点赞数
  - `dislikes` - 踩数
  - `likedBy` - 点赞用户列表
  - `dislikedBy` - 踩用户列表

**修复：页面结构调整** `[10:00]`
- 需求：页面结构固定为 stats-showcase → filter-section → review-list，不要单独的卡片
- 修改：
  - 删除单独的 `my-rating-card` 组件
  - 删除单独的 `add-rating-card` 组件
  - 我的评价在 review-list 内置顶显示
  - 未评价时 review-list 显示空状态

**修复：添加评价入口** `[10:05]`
- 需求：在 filter-section 和 review-list 之间添加入口，未评价时显示
- 实现：
  - 新增 `add-rating-entry` 组件
  - 优化设计：渐变背景、圆形图标、双行文案、箭头按钮
  - 点击进入评价界面

**修复：编辑模式隐藏展示内容** `[10:08]`
- 问题：编辑模式下 stats-showcase、filter-section、review-list 仍然显示
- 修复：为这些区域添加 `wx:if="{{!isEditing}}"` 条件

**修复：提交评价报错** `[10:10]`
- 问题：第一次评价时出现"更新错误"
- 原因：`isEditing=true` 时 action 为 `updateRating`，但 `myRating=null` 导致失败
- 修复：改为 `(isEditing && myRating) ? 'updateRating' : 'submitRating'`

**修复：保存/取消按钮同行显示** `[10:12]`
- 需求：修改评价时，取消和保存按钮在同一行展示
- 实现：
  - 新增 `save-cancel-wrap` 容器
  - 取消按钮：50%宽度，灰色边框
  - 保存按钮：50%宽度，蓝色渐变（激活时）
  - 两个按钮水平垂直高度一致

**优化：加载更多交互** `[10:15]`
- 问题：点击"加载更多"后没有反馈，突然显示新数据
- 优化：
  - 点击后隐藏"加载更多"按钮
  - 显示旋转loading图标 + "加载中..."文字
  - 加载完成后恢复显示
  - 添加点击反馈效果

**修复：图片路径错误** `[10:18]`
- 问题：渲染层报错，云存储路径被错误拼接本地路径前缀
- 错误路径：`/pages/rating/cloud://test-xxx.../img007.jpg`
- 正确路径：`cloud://test-xxx.../img007.jpg`
- 原因：数据库中存储的图片路径格式有问题
- 修复：添加 `fixImagePaths()` 方法自动修复路径格式
  - 检测路径是否包含 `cloud://` 但不是以 `cloud://` 开头
  - 提取 `cloud://` 及之后的部分
  - 过滤空路径
- 改动文件：miniprogram/pages/rating/rating.js
  - 新增 `fixImagePaths()` 方法
  - `loadExistingRating()` 加载时修复路径
  - `loadReviews()` 加载时修复路径

**修复：点赞/踩后我的评价丢失** `[10:20]`
- 问题：点击加载更多后，再点赞/踩其他评价，我的评价就不见了
- 原因：点赞/踩成功后调用 `loadReviews()` 没传参数，`append` 默认 `false`
  - 重新加载第一页数据，覆盖之前加载的所有数据
  - 如果我的评价在第二页，就会"丢失"
- 修复：点赞/踩成功后只更新本地状态，不重新加载整个列表
  - 找到被操作的评价，更新 `isLiked`/`isDisliked` 状态
  - 更新 `likes`/`dislikes` 数量
  - 互斥处理（点赞会取消踩，踩会取消点赞）
- 改动文件：miniprogram/pages/rating/rating.js
  - 修改 `onLike()` 方法：更新本地状态
  - 修改 `onDislike()` 方法：更新本地状态

**优化：添加评价入口组件设计** `[10:22]`
- 问题：原始设计太简陋
- 优化设计：
  - 渐变背景：蓝 → 紫 → 粉渐变
  - 圆形图标容器：毛玻璃效果（backdrop-filter）
  - 双行文案：标题 + 描述
  - 箭头按钮：引导用户点击
  - 阴影效果：增加立体感
- 样式特点：
  - `.add-rating-entry` - 渐变背景容器
  - `.entry-bg` - 背景渐变层
  - `.entry-icon-wrap` - 毛玻璃圆形图标
  - `.entry-info` - 双行文案容器
  - `.entry-arrow` - 箭头按钮

**提问：** `[09:51]`
1. 用户要求在 `docs/测试数据` 目录下添加评价系统测试数据40条
2. 要求不同用户的不同评价，不同评分

**修复：创建评价系统测试数据** `[09:55]`
- 需求：创建40条评价系统测试数据，覆盖不同用户、不同评分、不同评价内容
- 方案：方案A（模拟真实用户场景）
  - 40个不同用户，每用户1条评价
  - 评分分布：5分(12条) > 4分(10条) > 3分(8条) > 2分(6条) > 1分(4条)
  - 评价内容：30%详细描述、30%带标签、20%带图片、20%简单评价
  - 时间跨度：最近30天内随机分布

- 改动文件：
  - `docs/测试数据/ratings/test-ratings.json` - 新建评价测试数据（40条，NDJSON格式）

- 格式说明：采用 NDJSON 格式（每行一个JSON对象），与银行数据格式一致

- 数据结构：
  - `_id`: 评价唯一标识
  - `_openid`: 用户OpenID（40个不同用户）
  - `rating`: 评分（1-5）
  - `selectedTags`: 选中的标签数组
  - `comment`: 评价内容
  - `images`: 图片云存储路径
  - `createTime`: 创建时间
  - `likes/dislikes`: 点赞/踩数

- 标签选项：
  - `ui` → 🎨 界面美观
  - `easy` → ✨ 操作简单
  - `feature` → 🚀 功能实用
  - `speed` → ⚡ 运行流畅
  - `safe` → 🔒 数据安全
  - `free` → 🆓 完全免费

**修复：修正测试数据图片路径** `[10:05]`
- 问题：测试数据中的图片路径使用了错误的占位符格式
- 方案：统一使用真实云存储路径
  - 错误路径：`cloud://test-xxx.7465-xxx/rating-images/img00X.jpg`
  - 正确路径：`cloud://cloud1-d4gjxhe74a1deccaa.636c-cloud1-d4gjxhe74a1deccaa-1448877225/rating-images/1783304707969_4bq32ggpg.jpg`
- 影响：8条带图片的评价数据已全部修正

**提问：** `[11:15]`
1. 用户报告严重问题：点击赞或踩按钮时，整个页面有缩放回弹动画

**修复：点赞/踩页面缩放回弹问题** `[11:21]`
- 问题：点击赞或踩按钮时，整个页面出现缩放回弹动画
- 根因分析：
  1. `1be69c1` 提交将筛选栏从 `<scroll-view>` 改为 `<view>`
     - 之前嵌套的 `scroll-view` 会吸收/拦截触摸事件
     - 改为 `<view>` 后，触摸事件直接传递到外层 `scroll-view`
     - 外层 `scroll-view` 收到触摸事件后触发 iOS 弹性回弹效果
  2. `app.wxss` 中 `page` 元素的 `-webkit-overflow-scrolling: touch` 放大了这个效果
  3. `onLike/onDislike` 中使用 `setData` 替换整个数组导致列表重绘
- 修复方案：
  1. 给外层 `scroll-view` 添加 `bounces="{{false}}"` 禁用弹性回弹
  2. 在 `rating.wxss` 中覆盖 `page` 的滚动行为：`overflow: hidden; -webkit-overflow-scrolling: auto`
  3. 使用路径更新语法 `array[index].field` 只更新具体项，避免整个列表重绘
- 改动文件：
  - miniprogram/pages/rating/rating.wxml - scroll-view 添加 `bounces="{{false}}"`
  - miniprogram/pages/rating/rating.wxss - page 添加 overflow:hidden 和 -webkit-overflow-scrolling:auto
  - miniprogram/pages/rating/rating.js - 重写 onLike/onDislike 使用路径语法局部更新
- 效果：点赞/踩时整个页面不再缩放回弹

**提问：** `[12:20]`
1. 用户要求优化年度总结页面的 year-header 天记忆点样式

**修复：年度标题区域装饰效果优化** `[12:22]`
- 需求：为 year-header 添加精致的装饰效果，左右结构，不占用太多高度
- 设计方向：简洁精致风格 - 左右布局 + 渐变徽章 + 微动画
- 实现内容：
  1. 左侧：年份徽章（圆角方形渐变背景 + 光效动画）
  2. 中间：标题和副标题文字
  3. 右侧：三个装饰圆点（渐变色，不同透明度）
- 改动文件：
  - miniprogram/pages/yearly-summary/yearly-summary.wxml - 重构为左右结构
  - miniprogram/pages/yearly-summary/yearly-summary.wxss - 简洁样式和动画
- 设计特点：
  - 布局：左右结构，高度紧凑（88rpx 徽章）
  - 渐变：蓝 → 紫渐变徽章背景
  - 动画：2.5秒循环的光泽扫过效果
  - 装饰：右侧三个渐变圆点，增加层次感

**提问：** `[12:24]`
1. 用户希望点击右侧装饰点可以查看历史年度总结
2. 需要新建数据结构存储历史年度数据

**修复：新增历史年度选择功能** `[12:26]`
- 需求：点击年度标题右侧装饰点，弹出底部弹窗选择历史年度总结
- 实现内容：
  1. 新建年度选择器组件（year-picker）
     - 底部弹窗设计，带动画效果
     - 年度列表展示，支持选择
     - 空状态处理
  2. 修改年度总结页面
     - 添加年度选择器组件引用
     - 右侧装饰点绑定点击事件
     - 新增年度选择相关方法
  3. 数据结构设计
     - 生成可选年度列表（当前年份往前推5年）
     - 查询数据库中已有数据的年度
     - 显示年度数据状态

- 改动文件：
  - miniprogram/components/year-picker/ - 新建年度选择器组件
    - index.json - 组件配置
    - index.wxml - 组件模板
    - index.wxss - 组件样式
    - index.js - 组件逻辑
  - miniprogram/pages/yearly-summary/yearly-summary.json - 添加组件引用
  - miniprogram/pages/yearly-summary/yearly-summary.wxml - 添加点击事件和组件
  - miniprogram/pages/yearly-summary/yearly-summary.js - 添加年度选择逻辑

- 交互流程：
  1. 用户点击三点按钮
  2. 弹出底部弹窗（显示可选年度列表）
  3. 用户选择某个年度
  4. 关闭弹窗，加载对应年度数据
  5. 页面刷新显示选中年度的总结

**提问：** `[12:28]`
1. 用户指出需要后端和数据库设计
2. 需要支持年底自动生成 + 用户手动补生成
3. 需要查询全部有数据的年份

**修复：完善年度总结后端架构** `[12:31]`
- 需求：完整的年度总结数据存储和查询功能
- 实现内容：
  1. 新建云函数 `yearlySummary`
     - generate: 生成/更新年度总结
     - getYearList: 获取有数据的年度列表
     - getYearData: 获取指定年度数据
  2. 数据库集合设计
     - yearly_summaries: 存储年度总结数据
     - 包含完整的统计数据结构
  3. 前端调用逻辑优化
     - 使用新云函数获取数据
     - 支持生成历史年度总结

- 改动文件：
  - cloudfunctions/yearlySummary/ - 新建年度总结云函数
    - package.json - 依赖配置
    - index.js - 云函数逻辑
  - miniprogram/pages/yearly-summary/yearly-summary.js - 修改调用逻辑

- 数据结构：
  - yearly_summaries 集合
    - _openid: 用户标识
    - year: 年份
    - summaryData: 完整统计数据
    - createTime/updateTime: 时间戳

- 功能特点：
  - 支持年底自动生成 + 用户手动补生成
  - 查询全部有数据的年份
  - 实时计算或从缓存读取年度数据

**提问：** `[12:32]`
1. 用户要求彻底检查相关逻辑
2. 需要生成10年的测试数据

**修复：逻辑检查与测试数据生成** `[12:34]`
- 检查内容：
  1. 云函数逻辑：generate/getYearList/getYearData 三个接口
  2. 前端调用逻辑：年度选择、数据加载、生成年度总结
  3. 组件逻辑：底部弹窗、事件传递

- 测试数据生成：
  1. 创建 `docs/测试数据/yearly-summaries/` 目录
  2. 生成10年测试数据（2017-2026）
  3. 创建说明文档 README.md

- 改动文件：
  - docs/测试数据/yearly-summaries/test-yearly-summaries.json - 10年测试数据
  - docs/测试数据/yearly-summaries/README.md - 数据说明文档

- 数据特点：
  - 收入增长趋势：8.5万 → 16.5万
  - 储蓄率提升：15% → 32%
  - 记账率提升：67% → 98%
  - 2020年特殊：疫情影响
  - 2026年数据：截至7月的部分数据

**提问：** `[12:35]`
1. 用户指出 `_openid` 应该使用当前用户的值：`oFEshxuHd5E6Ikbhbpkz4WrVGI0c`

**修复：更新测试数据 _openid** `[12:36]`
- 问题：测试数据使用了自创的 `_openid`（test_user_001）
- 修复：将所有测试数据的 `_openid` 替换为当前用户值
- 改动文件：
  - docs/测试数据/yearly-summaries/test-yearly-summaries.json - 替换 _openid
  - docs/测试数据/yearly-summaries/README.md - 更新说明

**提问：** `[12:42]`
1. 用户要求检查年度报告点击后的历史年度列表获取逻辑

**修复：年度列表获取逻辑优化** `[12:44]`
- 问题1：年份过滤范围过窄
  - 原因：云函数 `getYearList` 限制 `year >= 2020`
  - 影响：2017-2019年的数据不显示
  - 修复：移除起始年份限制，显示所有有数据的年份

- 问题2：点击三点按钮时没有刷新列表
  - 原因：`onShowYearPicker` 只显示弹窗，不刷新数据
  - 影响：新增年度总结后，弹窗列表不会更新
  - 修复：点击时先调用 `loadYearList()` 刷新数据

- 改动文件：
  - cloudfunctions/yearlySummary/index.js - 移除年份限制
  - miniprogram/pages/yearly-summary/yearly-summary.js - 点击时刷新列表

**提问：** `[12:45]`
1. 用户反映数据库里已经有数据了，但年度列表不显示

**修复：年度列表获取逻辑再次优化** `[12:46]`
- 问题分析：
  1. `getYearList` 只从 `bills` 集合查询年份
  2. 测试数据在 `yearly_summaries` 集合，不在 `bills` 集合
  3. 导致即使有年度总结数据，也不会显示在列表中

- 修复方案：
  1. 同时从 `bills` 和 `yearly_summaries` 两个集合查询年份
  2. 合并两个集合的年份，去重后返回
  3. 添加分页查询 bills 集合（默认20条限制）
  4. 添加调试日志便于排查问题

- 改动文件：
  - cloudfunctions/yearlySummary/index.js - 双集合查询逻辑
  - miniprogram/pages/yearly-summary/yearly-summary.js - 添加调试日志

**提问：** `[12:47]`
1. 用户指出逻辑错误：历史年度和当年应该使用不同的数据来源

**修复：修正年度数据获取逻辑** `[12:48]`
- 逻辑梳理：
  - 历史年度（非当年）：从 `yearly_summaries` 表读取年度总结
  - 当年：从 `bills` 表实时计算（因为还没有年度总结）

- 修改内容：
  1. `getYearList`：
     - 从 `yearly_summaries` 查询历史年度总结
     - 从 `bills` 查询当年是否有账单
     - 分别添加到列表，标注不同状态

  2. `getYearData`：
     - 历史年度：直接读取 `yearly_summaries` 表
     - 当年：实时计算（调用 `calculateYearlyData`）

- 改动文件：
  - cloudfunctions/yearlySummary/index.js - 修正数据获取逻辑

**提问：** `[14:20]`
1. 用户要求检查使用天数和记账天数的统计是否正确
2. 开发调试很多天了，但天数都是1，不知道是否正常

**修复：使用天数统计修复** `[14:21]`
- 问题：使用天数总是显示为1
- 根因分析：
  1. 登录时（login.js:126-132）只保存了 avatarUrl、nickName、openid 到本地缓存
  2. 云数据库（user/index.js:36）已经正确保存了 createTime（首次登录时设置）
  3. 前端统计使用天数时（mine.js:119-126）读取本地缓存的 userInfo.createTime
  4. 本地缓存没有 createTime 字段，导致条件判断失败，返回默认值 1
- 修复方案：
  - 登录成功后调用 user.get 获取数据库中的完整用户信息（包含 createTime）
  - 将完整用户信息保存到本地缓存
  - 添加降级处理（获取失败时使用基本信息）
- 改动文件：
  - miniprogram/pages/login/login.js - 第 126-145 行，改为从数据库获取用户信息
- 效果：登录后本地缓存包含 createTime，使用天数统计正确

---

## 统计模块功能扩充

**提问：** `[14:25]`
1. 用户要求从用户角度评估统计菜单的功能可用性
2. 识别急需扩充完成的部分

**评估结果** `[14:30]`
- 已可用功能：日/月/季/年切换、环形图、分类排行、年度总结等
- 核心缺失：分类点击下钻、月度趋势图、环比对比
- 选定方案：方案A（渐进式补全），分三步实施

**修复：分类点击下钻功能** `[14:33]`
- 需求：点击统计页分类列表项，跳转查看该分类下的明细账单
- 实现方案：
  1. 云函数 `listBills` 新增 `category` 过滤参数
  2. 搜索页支持 `category` 和 `billType` URL参数，自动筛选展示
  3. 统计页分类列表添加点击事件，跳转搜索页并传递分类+日期范围参数
- 改动文件：
  - cloudfunctions/billFunctions/index.js - listBills 新增 category 过滤
  - miniprogram/pages/search/search.js - 新增 categoryFilter/billTypeFilter 参数处理
  - miniprogram/pages/stats/stats.js - 新增 goToCategoryDetail 方法
  - miniprogram/pages/stats/stats.wxml - stats-item 添加 bindtap 和箭头图标
  - miniprogram/pages/stats/stats.wxss - 添加 stats-item:active 和箭头样式

**修复：统计页月度趋势柱状图** `[14:33]`
- 需求：年度模式下展示月度支出/收入趋势柱状图
- 实现方案：
  1. 云函数新增 `getMonthlyTrend` action，按月聚合年度数据
  2. 新建工具函数 `utils/monthlyTrend.js` 处理趋势数据计算
  3. 新建工具函数 `utils/statsDate.js` 抽取日期处理逻辑（stats.js 降至290行）
  4. 统计页年度模式下展示 CSS 柱状图（支出红色/收入绿色）
- 改动文件：
  - cloudfunctions/billFunctions/index.js - 新增 getMonthlyTrend 函数
  - miniprogram/utils/monthlyTrend.js - 新建月度趋势数据处理工具
  - miniprogram/utils/statsDate.js - 新建日期工具函数
  - miniprogram/pages/stats/stats.js - 新增 loadMonthlyTrend 方法，重构为工具函数调用
  - miniprogram/pages/stats/stats.wxml - 新增月度趋势柱状图区域
  - miniprogram/pages/stats/stats.wxss - 新增柱状图样式

**修复：年度总结页月度趋势卡片** `[14:33]`
- 需求：年度总结页添加月度支出/收入趋势可视化
- 实现：复用 monthlyTrend 工具函数，添加月度趋势卡片
- 改动文件：
  - miniprogram/pages/yearly-summary/yearly-summary.js - 新增 loadMonthlyTrend 方法
  - miniprogram/pages/yearly-summary/yearly-summary.wxml - 新增月度趋势卡片
  - miniprogram/pages/yearly-summary/yearly-summary.wxss - 新增柱状图样式

**代码自查与多角度审查修复** `[14:40]`
- 审查发现并修复以下问题：

| 严重度 | 问题 | 修复 |
|--------|------|------|
| 🔴 HIGH | search.js 使用不存在的 `getBills` action | 改为 `list` |
| 🔴 HIGH | `getMonthlyTrend` 受100条记录限制 | 改用聚合管道 `aggregate()` |
| 🟡 MEDIUM | 季度模式初始化使用了"日"范围 | 修复 ternary 条件 |
| 🟡 MEDIUM | `getDisplayDate`/`getDateRange` 无 default 分支 | 添加 default 返回值 |
| 🟢 LOW | `expenseHeight`/`incomeHeight` 类型不一致 | 统一为字符串 |
| 🟢 LOW | `loadMonthlyTrend()` 未 await 导致布局闪烁 | await 后再隐藏骨架屏 |
| 🟢 LOW | 月份解析假设补零格式 | 添加正则清洗兼容 |

- 改动文件：
  - miniprogram/pages/search/search.js - 修正 action 名称
  - cloudfunctions/billFunctions/index.js - 聚合管道 + 月份兼容
  - miniprogram/pages/stats/stats.js - 修复季度初始化条件
  - miniprogram/utils/statsDate.js - 添加 default 分支
  - miniprogram/utils/monthlyTrend.js - 统一类型
  - miniprogram/pages/yearly-summary/yearly-summary.js - await 趋势加载

**修复：模拟器长时间无响应** `[14:50]`
- 问题：模拟器长时间无响应，疑似死循环或复杂运算
- 根因：`getMonthlyTrend` 使用了微信云数据库不支持的 `aggregate()` 聚合管道语法
  - `$.substr`、`$.sum` 等聚合操作符在微信云开发中语法不同
  - 导致云函数执行卡死，前端长时间等待无响应
- 修复方案：
  1. 移除 `aggregate()` 聚合管道，改用分页循环查询（每批100条）
  2. 顺带修复 `getStatsByDateRange` 的100条记录限制，同样改用分页查询
  3. 月份解析改用 `split('-')` 替代 `substring(5,7)`，兼容非补零格式
- 改动文件：
  - cloudfunctions/billFunctions/index.js - getMonthlyTrend 和 getStatsByDateRange 改用分页查询

**提问：** `[15:05]`
1. 用户要求统计界面的 overview-card 的 animate-slideUp 部分参照账单界面的 hero-card 进行实现

**修复：统计页概览卡片动画优化** `[15:07]`
- 需求：将统计页 overview-card 的动画效果对齐账单页 hero-card 的实现方式
- 对比分析：

| 对比项 | bills 的 hero-card | stats 的 overview-card（修改前） |
|--------|-------------------|------|
| CSS 动画延迟 | `.hero-card.animate-fadeIn { animation-delay: 0s }` 和 `.hero-card.animate-slideUp { animation-delay: 0.08s }` | 无（仅靠 style 内联） |
| WXML 类名 | `class="hero-card animate-fadeIn animate-slideUp"` | `class="overview-card animate-slideUp"` |
| 特点 | 同时使用 fadeIn + slideUp 双重动画 | 只有 slideUp |

- 修改内容：
  1. WXML：overview-card 同时添加 `animate-fadeIn` 和 `animate-slideUp` 类名，移除内联 style
  2. CSS：为 overview-card 添加 `will-change: transform, opacity` 和 `transform-origin: center top`
  3. CSS：添加 `.overview-card.animate-fadeIn` 和 `.overview-card.animate-slideUp` 动画延迟样式

- 改动文件：
  - miniprogram/pages/stats/stats.wxml - overview-card 类名修改
  - miniprogram/pages/stats/stats.wxss - 添加动画相关样式

**提问：** `[15:10]`
1. 用户反馈从统计界面的分类明细点击后的搜索界面有功能性问题
2. 搜索框是乱码

**修复：搜索页分类名称乱码问题** `[15:12]`
- 问题：从统计页点击分类明细跳转到搜索页时，搜索框显示乱码（如 `%E4%BD%8F%E5%AE%BF`）
- 根因分析：
  - 统计页跳转时使用 `encodeURIComponent(category)` 编码分类名称（stats.js:385）
  - 搜索页接收参数时直接使用 `options.category`，没有解码（search.js:37）
  - 导致中文分类名显示为 URL 编码后的乱码
- 修复方案：在搜索页 onLoad 中使用 `decodeURIComponent` 解码 category 参数
- 改动文件：
  - miniprogram/pages/search/search.js - 第37行，`options.category` 改为 `decodeURIComponent(options.category || '')`
- 效果：搜索框正确显示中文分类名称

**提问：** `[15:13]`
1. 用户要求搜索界面的列表项显示内容参照账单界面
2. 如果备注没有则显示默认内容，账户也是

**修复：搜索页列表项显示优化** `[15:15]`
- 需求：搜索页列表项显示内容对齐账单页，包括账户标签和默认备注
- 实现内容：
  1. WXML 结构优化
     - 添加 `bill-name-row` 容器，包含分类名和账户标签
     - 添加 `bill-account-tag` 显示账户名称（无账户显示灰色样式）
     - 备注为空时显示"暂无备注"（斜体灰色）
  2. CSS 样式添加
     - `.bill-name-row` - 分类名和账户标签水平布局
     - `.bill-account-tag` - 账户标签样式（蓝色背景）
     - `.bill-account-tag.no-account` - 无账户时灰色样式
     - `.bill-note.no-note` - 无备注时斜体样式
  3. JS 逻辑添加
     - 新增 `fetchAccountNames()` 方法 - 批量获取账户名称
     - 调用 `accountFunctions` 云函数的 `listByIds` 接口
  4. 云函数扩展
     - `accountFunctions/index.js` - 新增 `listByIds` 方法

- 改动文件：
  - miniprogram/pages/search/search.wxml - 列表项结构优化
  - miniprogram/pages/search/search.wxss - 添加账户标签和备注样式
  - miniprogram/pages/search/search.js - 添加 fetchAccountNames 方法
  - cloudfunctions/accountFunctions/index.js - 新增 listByIds 方法

- 效果：
  - 搜索页列表项显示账户标签（如"微信"、"支付宝"）
  - 无账户时显示灰色"无账户"标签
  - 无备注时显示"暂无备注"斜体文字
  - 与账单页显示风格完全一致

**提问：** `[15:16]`
1. 用户要求月、季度、年模式下，列表项金额下方显示小号字体的日期（如2026-7-7）
2. 这样在大的分类下能清楚知道每笔账单的具体日期

**修复：搜索页列表项添加日期显示** `[15:18]`
- 需求：月/季度/年模式下，列表项金额下方显示小号字体的具体日期
- 实现内容：
  1. WXML 修改
     - 在 `bill-right` 区域的金额下方添加 `bill-date` 日期显示
     - 使用 `wx:if="{{viewMode !== 'day'}}"` 条件，仅在非日模式下显示
  2. CSS 添加
     - `.bill-date` - 小号字体（22rpx）、灰色、下方间距

- 改动文件：
  - miniprogram/pages/search/search.wxml - 添加 bill-date 元素
  - miniprogram/pages/search/search.wxss - 添加日期样式

- 效果：
  - 日模式：不显示日期（分组已是按日）
  - 月/季度/年模式：金额下方显示如"2026-7-6"的日期
  - 用户可以清楚知道每笔账单的具体日期

**提问：** `[15:27]`
1. 用户要求彻底删除统计界面 overview-card 及 animate-fadeIn/animate-slideUp 动画
2. 将账单界面 hero-card 100% 完整复制过来，包括结构、样式、点击效果、动画

**修复：统计页 overview-card 替换为 hero-card** `[15:30]`
- 需求：删除 overview-card 及其动画，替换为首页 hero-card 的完整实现
- 实现内容：
  1. WXML 结构替换
     - 删除 overview-card 及其 animate-fadeIn/animate-slideUp 类
     - 移除其他元素上的 animate-slideUp 类和内联 animation-delay
     - 复制首页 hero-card 完整结构：默认三列 + 展开状态
  2. WXSS 样式替换
     - 删除 overview-card 相关全部样式（overview-grid、overview-cell、overview-dot、overview-info、overview-label、overview-amount 等）
     - 添加 hero-card 完整样式：渐变背景、伪元素装饰、展开/收起动画（fadeIn、slideUp keyframes）
  3. JS 逻辑适配
     - 新增 formatAmountWithUnit 导入
     - 新增 expandedAmount 数据字段（控制展开状态）
     - 新增 toggleAmountExpand 方法（点击展开/收起金额）
     - loadStats 中新增带单位金额数据（totalExpenseValue/Unit 等）
     - resetTemporaryStates 中重置 expandedAmount

- 改动文件：
  - miniprogram/pages/stats/stats.wxml - overview-card → hero-card 结构
  - miniprogram/pages/stats/stats.wxss - overview-card 样式 → hero-card 样式
  - miniprogram/pages/stats/stats.js - 新增 formatAmountWithUnit、expandedAmount、toggleAmountExpand

- 效果：
  - 统计页概览卡片与账单页 hero-card 完全一致
  - 蓝色渐变背景 + 装饰伪元素
  - 点击金额可展开显示完整数值（带 fadeIn + slideUp 动画）
  - 默认状态显示带单位的简短金额（如 1.2 /万）
  - 展开状态显示完整元为单位金额

**提问：** `[15:35]`
1. 用户发现统计界面日分类的默认起始日期是今天（如7月6日），而不是当月1号（7月1日）
2. 要求修改为：起始日期默认是当月1号，结束日期是今天
3. 账单界面的日分类保持不变（起始日期还是今天）

**修复：统计页日分类默认起始日期修改** `[15:36]`
- 问题：统计页日分类默认起始日期是今天（如7月6日），不方便查看月初到今天的统计
- 根因分析：
  - `getQuickTabDateRange` 函数中 `day` 类型的 `rangeStartDate` 设置为今天
  - 应该设置为当月1号，方便用户查看月初至今的统计
- 修复方案：
  - 修改 `rangeStartDate` 为 `${realYear}-${realMonthStr}-01`（当月1号）
  - `rangeEndDate` 保持为今天
  - 账单页（index.js）的日分类逻辑保持不变
- 改动文件：
  - miniprogram/pages/stats/stats.js - 第57-59行，修改 `getQuickTabDateRange` 函数的 `day` 类型处理
- 效果：
  - 今天是7月6日，统计页日分类默认时间范围：7月1日 ~ 7月6日
  - 账单页日分类默认时间范围：7月6日（保持不变）

**提问：** `[15:37]`
1. 用户反馈统计界面日/月/季/年分类切换的选中效果是黑色，很难看
2. 收入/支出切换的选中状态也需要优化

**修复：统计页分类切换选中状态样式优化** `[15:38]`
- 问题：日/月/季/年分类切换和收入/支出切换的选中状态使用黑色背景（`var(--color-text)` = `#1A1D26`），视觉效果沉闷
- 优化方案：采用蓝色系渐变，与统计页hero-card的蓝色主题保持一致
- 修改内容：
  1. `.quick-tab.active`（日/月/季/年分类）
     - 背景：黑色 → 蓝色渐变 `linear-gradient(135deg, #2563EB 0%, #1E40AF 100%)`
     - 阴影：增强为 `0 4rpx 16rpx rgba(37, 99, 235, 0.3)`
  2. `.tab-item.active`（收入/支出切换）
     - 背景：黑色 → 蓝色渐变 `linear-gradient(135deg, #2563EB 0%, #1E40AF 100%)`
     - 阴影：增强为 `0 4rpx 16rpx rgba(37, 99, 235, 0.3)`
- 改动文件：
  - miniprogram/pages/stats/stats.wxss - 修改 `.quick-tab.active` 和 `.tab-item.active` 样式
  - miniprogram/pages/index/index.wxss - 修改 `.quick-tab.active` 样式（账单页同步）
- 效果：
  - 选中状态使用蓝色渐变，与页面整体蓝色主题协调
  - 渐变效果增加层次感和现代感
  - 阴影效果增加立体感
  - 统计页和账单页的分类切换样式保持一致

---

## 账单模块

**提问：** `[15:53]`
1. 用户反馈账单界面滚动异常：列表项之间滑动可以滚动，但在列表空白区域滑动无法滚动

**修复：账单列表空白区域滚动问题** `[15:53]`
- 问题：在账单列表中，只有在列表项（账单卡片）上滑动才能滚动，但在列表项之间的空白区域滑动无法触发滚动
- 根因分析：
  - WXML 中所有账单卡片（`.swipe-card`）绑定了 `catchtouchstart`/`catchtouchmove`/`catchtouchend`
  - `catch` 事件会阻止事件冒泡，导致触摸事件无法传递到父级 `scroll-view`
  - 空白区域没有绑定触摸事件，无法触发 `scroll-view` 的滚动
- 修复方案：
  - 将 `catchtouchstart`/`catchtouchmove`/`catchtouchend` 改为 `bindtouchstart`/`bindtouchmove`/`bindtouchend`
  - `bind` 事件允许冒泡，触摸事件可以传递到 `scroll-view`，触发滚动
  - 左滑删除功能仍正常工作（onTouchMove 中的逻辑不受影响）
- 改动文件：
  - miniprogram/pages/index/index.wxml - 日/月/季/年四种模式下的 swipe-card 共3处修改
- 效果：
  - 列表空白区域可以正常滚动
  - 列表项上的滑动操作仍正常
  - 左滑删除功能不受影响

---

## UI优化

**提交：优化页面布局与移除版本信息** `[16:04]`
- 提交哈希：`7556b0e`
- 提交信息：style: 优化页面布局与移除版本信息
- 修改内容：
  1. account.wxss: 添加底部内边距 `padding-bottom: 100rpx` 避免内容被底部导航遮挡
  2. stats.wxss: 添加底部内边距 `padding-bottom: 100rpx` 避免内容被底部导航遮挡
  3. mine.wxml: 移除版本信息显示区域（SpendNote v1.0.0 和副标题）
- 改动文件：
  - miniprogram/pages/account/account.wxss
  - miniprogram/pages/stats/stats.wxss
  - miniprogram/pages/mine/mine.wxml

---

## 账户模块

**提问：** `[16:07]`
1. 用户反馈账户界面的借款记账部分加载中状态有问题
2. 骨架屏高度与实际内容高度不一致，导致加载完成后突然的高度变化

**修复：借款记账加载骨架屏优化** `[16:08]`
- 问题：加载中状态只有一个spinner+文字，高度约200rpx，加载完成后显示两个loan-item，高度约350rpx，导致高度突然跳动
- 优化方案：创建与实际内容结构一致的骨架屏
- 实现内容：
  1. WXML结构修改
     - 将简单的section-loading替换为loan-skeleton骨架屏
     - 骨架屏包含两个skeleton-item，模拟借出/借入两个loan-item
     - 每个骨架项包含圆形图标占位、标题占位、副标题占位、金额占位
  2. WXSS样式添加
     - .loan-skeleton - 骨架屏容器
     - .skeleton-item - 禁用点击事件
     - .skeleton-circle - 圆形图标占位（80rpx×80rpx）
     - .skeleton-text - 文字占位（带脉冲动画）
     - .skeleton-title - 标题占位（80rpx×28rpx）
     - .skeleton-subtitle - 副标题占位（60rpx×22rpx）
     - .skeleton-amount - 金额占位（120rpx×32rpx）
     - @keyframes skeleton-pulse - 脉冲动画效果

- 改动文件：
  - miniprogram/pages/account/account.wxml - 骨架屏结构
  - miniprogram/pages/account/account.wxss - 骨架屏样式

- 效果：
  - 加载中状态显示两个灰色骨架项，与实际内容高度完全一致
  - 骨架项带脉冲动画效果，视觉体验更流畅
  - 加载完成后无高度跳动，页面平滑过渡

**提问：** `[16:09]`
1. 用户反馈统计界面年度分类下的月度趋势图交互功能不足
2. 需要添加Y轴刻度、点击信息卡片、趋势线

**修复：月度趋势图交互优化** `[16:13]`
- 问题：月度趋势图只有简单的柱状图，没有Y轴刻度、点击交互、趋势线
- 优化方案：添加完整的交互功能
- 实现内容：
  1. WXML结构修改
     - 添加Y轴刻度区域（y-axis），显示5个刻度值
     - 添加网格线（grid-lines），辅助视觉对齐
     - 添加趋势线（trend-lines），连接各月数据点
     - 添加数据点（trend-dot），标记支出/收入
     - 添加点击信息卡片（trend-tooltip），显示当月详细信息
     - 柱状图添加点击事件（onBarTap）
  2. WXSS样式添加
     - .bar-chart-container - 柱状图容器布局
     - .y-axis - Y轴刻度样式
     - .y-axis-label - 刻度文字样式
     - .grid-lines - 网格线样式
     - .grid-line - 单条网格线样式
     - .trend-lines - 趋势线容器
     - .trend-line - 趋势线样式（支出红色/收入绿色）
     - .trend-dot - 数据点样式
     - .trend-tooltip - 信息卡片样式
     - .tooltip-header - 卡片头部样式
     - .tooltip-content - 卡片内容样式
     - .tooltip-item - 单项样式
     - .tooltip-value - 金额样式
     - .bar.selected - 选中柱状图高亮
     - .bar-label.selected - 选中标签高亮
  3. JS逻辑添加
     - 修改 loadMonthlyTrendData 函数
       - 计算Y轴刻度（5个刻度线）
       - 计算每个数据点位置（dotLeft、expenseDotBottom、incomeDotBottom）
       - 计算结余数据（balance、balanceStr）
     - 添加 onBarTap 方法 - 点击柱状图显示信息卡片
     - 添加 hideTooltip 方法 - 隐藏信息卡片
     - 修改 loadMonthlyTrend 方法 - 传递新数据

- 改动文件：
  - miniprogram/pages/stats/stats.wxml - 添加Y轴、趋势线、信息卡片结构
  - miniprogram/pages/stats/stats.wxss - 添加相关样式
  - miniprogram/pages/stats/stats.js - 添加交互逻辑

- 效果：
  - 左侧显示Y轴刻度（5个刻度线），金额从0到最大值
  - 显示网格线，辅助视觉对齐
  - 点击柱状图时显示信息卡片，包含当月支出、收入、结余
  - 选中的柱状图高亮显示，标签加粗
  - 信息卡片带关闭按钮，点击关闭

**修复：去除月度趋势图趋势线** `[16:14]`
- 需求：去除趋势线功能，保留Y轴刻度、网格线、点击信息卡片
- 修改内容：
  1. WXML：移除趋势线容器（trend-lines）和数据点（trend-dot）
  2. WXSS：移除趋势线相关样式（.trend-lines、.trend-line、.trend-dot等）
  3. JS：移除趋势线相关数据（expenseLinePath、incomeLinePath、dotLeft等）
- 改动文件：
  - miniprogram/pages/stats/stats.wxml - 移除趋势线结构
  - miniprogram/pages/stats/stats.wxss - 移除趋势线样式
  - miniprogram/pages/stats/stats.js - 移除趋势线数据
- 效果：
  - 保留Y轴刻度和网格线
  - 保留点击信息卡片功能
  - 去除趋势线和数据点，界面更简洁

**修复：信息卡片点击外部自动关闭** `[16:16]`
- 需求：点击月度趋势以外区域时，自动关闭信息卡片
- 实现方案：
  1. 在 trend-card 上添加 bindtap="hideTooltip" 事件
  2. 在 bar-group 上使用 catchtap="onBarTap" 阻止事件冒泡
  3. 点击柱状图时阻止冒泡，不会触发 hideTooltip
  4. 点击其他区域时触发 hideTooltip，自动关闭信息卡片
- 改动文件：
  - miniprogram/pages/stats/stats.wxml - 修改事件绑定
- 效果：
  - 点击柱状图：显示/切换信息卡片
  - 点击信息卡片以外区域：自动关闭信息卡片
  - 点击关闭按钮：关闭信息卡片

**提问：** `[16:30]`
1. 用户要求统计界面收入/支出切换时，图表和分类明细部分显示加载中骨架屏

**修复：统计页收入/支出切换骨架屏** `[16:32]`
- 需求：切换收入/支出时，甜甜圈图表和分类明细部分显示骨架屏加载状态
- 实现内容：
  1. JS 修改（stats.js）
     - `switchTab()` 函数：切换时清空 categoryStats、displayStats、donutGradient
     - 骨架屏会在 loading=true 且无数据时显示
  2. WXML 修改（stats.wxml）
     - 添加骨架屏结构：甜甜圈骨架 + 分类明细骨架
     - 条件：`wx:if="{{loading && categoryStats.length === 0}}"`
     - 甜甜圈骨架：圆形占位 + 金额/标签占位 + 图例占位（5项）
     - 分类明细骨架：4行列表项占位（排名、图标、名称、进度条、金额）
  3. WXSS 修改（stats.wxss）
     - 添加骨架屏样式和脉冲动画
     - skeleton-pulse 动画：1.5秒循环，透明度0.4-1
     - 骨架元素使用 var(--color-border) 背景色
- 改动文件：
  - miniprogram/pages/stats/stats.js - switchTab 清空数据
  - miniprogram/pages/stats/stats.wxml - 添加骨架屏结构
  - miniprogram/pages/stats/stats.wxss - 添加骨架屏样式
- 效果：
  - 切换收入/支出时显示骨架屏加载状态
  - 数据加载完成后平滑过渡到真实内容
  - 骨架屏结构与实际内容一致，避免高度跳动

**提问：** `[16:40]`
1. 用户反馈年度总结界面选择年度部分元素右侧超出屏幕

**修复：年度选择器宽度溢出问题** `[16:41]`
- 问题：年度选择器列表项文字过长时超出屏幕右侧
- 根因分析：
  1. `.year-left` 没有设置 flex: 1 和 overflow: hidden
  2. `.year-info` 没有限制宽度，文字可能溢出
  3. `.year-label` 和 `.year-desc` 没有文本溢出处理
- 修复方案：
  1. `.year-left` 添加 flex: 1 和 overflow: hidden
  2. `.year-info` 添加 flex: 1 和 overflow: hidden
  3. `.year-label` 和 `.year-desc` 添加 text-overflow: ellipsis 和 white-space: nowrap
- 改动文件：
  - miniprogram/components/year-picker/index.wxss - 修复宽度溢出
- 效果：
  - 年度列表项宽度受限于屏幕
  - 文字过长时显示省略号
  - 不再超出屏幕右侧

**提问：** `[16:45]`
1. 用户要求年度总结页面的月度趋势部分参考统计界面的月度趋势
2. 需要添加左侧Y轴刻度和点击信息面板

**修复：年度总结页月度趋势交互优化** `[16:48]`
- 需求：年度总结页的月度趋势图添加Y轴刻度、网格线、点击信息卡片
- 实现内容：
  1. JS逻辑修改（yearly-summary.js）
     - 新增 formatAmount 金额格式化函数
     - 修改 loadMonthlyTrendData 函数：计算Y轴刻度、计算结余数据
     - 修改 loadMonthlyTrend 方法：传递 yAxisLabels 数据
     - 新增 onBarTap 方法：点击柱状图显示信息卡片
     - 新增 hideTooltip 方法：隐藏信息卡片
     - 新增数据字段：yAxisLabels、showTooltip、selectedMonth、tooltipData
  2. WXML结构修改（yearly-summary.wxml）
     - trend-card 添加 bindtap="hideTooltip" 事件
     - 新增点击信息卡片结构（trend-tooltip）
     - 新增柱状图容器（bar-chart-container）
     - 新增Y轴刻度区域（y-axis），显示5个刻度值
     - 新增网格线（grid-lines），辅助视觉对齐
     - 柱状图添加 catchtap="onBarTap" 点击事件
     - 柱状图添加选中状态样式（selected）
  3. WXSS样式修改（yearly-summary.wxss）
     - 新增点击信息卡片样式（.trend-tooltip、.tooltip-header、.tooltip-content等）
     - 新增柱状图容器样式（.bar-chart-container）
     - 新增Y轴刻度样式（.y-axis、.y-axis-label）
     - 新增网格线样式（.grid-lines、.grid-line）
     - 新增柱状图选中状态样式（.bar.selected、.bar-label.selected）
     - 优化柱状图过渡动画（添加 opacity 过渡）
- 改动文件：
  - miniprogram/pages/yearly-summary/yearly-summary.js - 添加交互逻辑
  - miniprogram/pages/yearly-summary/yearly-summary.wxml - 添加交互结构
  - miniprogram/pages/yearly-summary/yearly-summary.wxss - 添加交互样式
- 效果：
  - 左侧显示Y轴刻度（5个刻度线），金额从0到最大值
  - 显示网格线，辅助视觉对齐
  - 点击柱状图时显示信息卡片，包含当月支出、收入、结余
  - 选中的柱状图高亮显示，标签加粗
  - 信息卡片带关闭按钮，点击关闭
  - 点击其他区域自动关闭信息卡片
  - 与统计页面的月度趋势交互完全一致

**提问：** `[16:52]`
1. 用户反馈年度标题右侧的三个装饰点太小，用户难以点击

**修复：年度选择器入口按钮优化** `[16:54]`
- 问题：year-deco 区域的三个小圆点作为可点击入口，面积太小且不够明显
- 根因分析：
  1. 三个圆点只有 8rpx × 8rpx，远小于微信推荐的 88rpx 最小点击区域
  2. 没有明显的按钮样式，用户不知道可以点击
  3. 缺少点击反馈，用户无法确认是否点击成功
- 优化方案：简化为下拉箭头按钮，去除装饰圆点
- 实现内容：
  1. WXML结构修改（yearly-summary.wxml）
     - 移除三个装饰圆点
     - 只保留 deco-arrow 下拉箭头图标
  2. WXSS样式修改（yearly-summary.wxss）
     - year-deco 简化为纯按钮样式：浅蓝色背景、圆角、内边距
     - 添加 :active 状态：背景色加深 + 缩放效果（点击反馈）
     - 删除 deco-dots 和 deco-dot 相关样式
     - 优化 deco-arrow 下拉箭头大小（10rpx × 12rpx）
- 改动文件：
  - miniprogram/pages/yearly-summary/yearly-summary.wxml - 简化结构
  - miniprogram/pages/yearly-summary/yearly-summary.wxss - 简化样式
- 效果：
  - 点击区域约 52rpx × 52rpx，易于点击
  - 简洁的下拉箭头按钮，功能明确
  - 点击时有缩放+变色反馈，提升交互体验
  - 界面更简洁，去除冗余装饰

---

## 年度总结模块

**提问：** `[17:35]`
1. 用户询问年度总结的生成逻辑：是一年最后一天还是每年1月1日总结上一年的
2. 发现设计缺陷：历史年份如果没有手动生成总结，跨年后无法查看

**修复：年度总结自动兜底生成** `[17:36]`
- 问题：年度总结没有自动保存机制，历史年份如果没有手动生成总结，跨年后无法查看
- 根因分析：
  1. `getYearData` 函数中，历史年份从 `yearly_summaries` 表读取
  2. 如果该年份没有保存的总结，直接返回错误
  3. 导致用户在2027年无法查看2026年的年度总结（如果2026年未手动生成）
- 修复方案：在云函数中添加自动兜底生成逻辑
- 实现内容：
  1. 修改 `cloudfunctions/yearlySummary/index.js` 的 `getYearData` 函数
  2. 当历史年份没有总结时，自动调用 `calculateYearlyData` 计算数据
  3. 将计算结果保存到 `yearly_summaries` 表（标记 `isAutoGenerated: true`）
  4. 返回计算结果给前端
- 改动文件：
  - cloudfunctions/yearlySummary/index.js - getYearData 函数添加自动兜底生成逻辑
- 效果：
  - 用户访问历史年份时，如果没有总结会自动生成并保存
  - 保证用户始终可以查看任何有数据的年份
  - 自动生成的总结标记为 `isAutoGenerated: true`，便于区分

**修复：年度总结存储月度趋势数据** `[17:38]`
- 问题：历史年份的年度总结没有月度趋势数据，导致查看历史年份时月度趋势图表为空
- 根因分析：
  1. 年度总结存储的 `summaryData` 不包含月度趋势数据
  2. 前端每次调用 `getMonthlyTrend` 云函数获取月度趋势
  3. 历史年份的账单数据可能已被清理，导致查询不到数据
- 修复方案：在生成年度总结时，同时计算并存储月度趋势数据
- 实现内容：
  1. 修改 `cloudfunctions/yearlySummary/index.js` 的 `calculateYearlyData` 函数
     - 添加按月份聚合账单数据的逻辑
     - 计算每月的收入、支出、笔数
     - 计算Y轴刻度（用于前端图表）
     - 将月度趋势数据添加到返回的 `summaryData` 中
  2. 修改 `miniprogram/pages/yearly-summary/yearly-summary.js` 的 `loadYearlyData` 方法
     - 从云函数返回的 `data` 中提取月度趋势数据
     - 如果年度总结中没有月度趋势数据，再调用云函数获取（兼容旧数据）
- 改动文件：
  - cloudfunctions/yearlySummary/index.js - calculateYearlyData 函数添加月度趋势计算
  - miniprogram/pages/yearly-summary/yearly-summary.js - loadYearlyData 方法添加月度趋势数据读取
- 效果：
  - 新生成的年度总结包含完整的月度趋势数据
  - 历史年份查看时，月度趋势图表正常显示
  - 兼容旧数据：如果年度总结中没有月度趋势，仍会调用云函数获取

**修复：更新年度总结测试数据** `[17:41]`
- 需求：修改测试数据，让一部分有月度趋势参数，一部分缺失，用于测试兼容性逻辑
- 实现内容：
  1. 修改 `docs/测试数据/yearly-summaries/test-yearly-summaries.json`
     - 2017-2022年：没有月度趋势数据（模拟旧数据）
     - 2023-2026年：有月度趋势数据（模拟新数据）
  2. 更新 `docs/测试数据/yearly-summaries/README.md`
     - 添加月度趋势数据结构说明
     - 在数据趋势表中添加月度趋势列
- 改动文件：
  - docs/测试数据/yearly-summaries/test-yearly-summaries.json - 更新测试数据
  - docs/测试数据/yearly-summaries/README.md - 更新说明文档
- 效果：
  - 测试兼容性逻辑：旧数据（无月度趋势）会调用云函数获取，新数据（有月度趋势）直接读取
  - 2017-2022年：查看时月度趋势图表为空（触发兼容逻辑）
  - 2023-2026年：查看时月度趋势图表正常显示

**修复：年度总结月度趋势图表不显示** `[17:45]`
- 问题：选择有月度趋势参数的历史总结时，图表没有显示相关内容
- 根因分析：
  1. 从年度总结中读取的 `monthlyTrend` 数据只有 `expense` 和 `income`，没有 `expenseHeight` 和 `incomeHeight`
  2. WXML中柱状图需要 `item.expenseHeight` 和 `item.incomeHeight` 来设置高度
  3. 这两个字段只在 `loadMonthlyTrendData` 函数中计算，从年度总结读取时没有计算
- 修复方案：在 `loadYearlyData` 方法中，从年度总结读取月度趋势数据后，计算 `expenseHeight` 和 `incomeHeight`
- 实现内容：
  1. 修改 `miniprogram/pages/yearly-summary/yearly-summary.js` 的 `loadYearlyData` 方法
     - 如果年度总结中有月度趋势数据，计算每个数据点的 `expenseHeight` 和 `incomeHeight`
     - 计算公式：`height = (amount / maxMonthlyAmount) * 100`
  2. 保持兼容逻辑：如果没有月度趋势数据，仍调用云函数获取
- 改动文件：
  - miniprogram/pages/yearly-summary/yearly-summary.js - loadYearlyData 方法添加柱状图高度计算
- 效果：
  - 选择有月度趋势的历史总结时，图表正常显示柱状图
  - 柱状图高度根据数据比例正确计算
  - 兼容旧数据：如果没有月度趋势，仍会调用云函数获取
